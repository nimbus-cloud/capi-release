set -e

export GOROOT=$(readlink -nf /var/vcap/packages/golang1.7)
export PATH=$GOROOT/bin:$PATH
export GOPATH=$PWD
mkdir $GOPATH/src
mv $GOPATH/* $GOPATH/src || true

go install github.com/cloudfoundry/blobstore_url_signer
cp -a $PWD/bin/blobstore_url_signer $BOSH_INSTALL_TARGET
